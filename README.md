# Planes package for laravel

the data from this package based on https://openflights.org/data.html#plane

how to install:
1. install via composer `composer require webappid/laravel-plane`
2. seeding the data `webappid:plane:seed`